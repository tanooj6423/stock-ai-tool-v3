# crypto/ — environment template.
# The v1 core build requires ZERO API keys: all sources (Binance public REST,
# Coin Metrics Community, Alternative.me, yfinance) are free and keyless.
# Never commit a real .env file.

# Optional, unused in v1 (reserved for future macro extras):
# FRED_API_KEY=
